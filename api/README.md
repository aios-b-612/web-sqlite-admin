# web-sqlite-admin-api

API Rust/Actix do admin SQLite Octor. Opera ficheiros em `LOCATION` (compatível com phpLiteAdmin / `platform-hosting`).

```bash
cp .env.example .env
mkdir -p /tmp/octor-sqlite-admin
cargo run
```

Ver README na raiz do monorepo-dual: [`../README.md`](../README.md).
