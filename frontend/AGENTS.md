# AGENTS.md — `web-sqlite-admin` / frontend

Next.js (template-frontend). Modo padrão: `NEXT_PUBLIC_SQLITE_PANEL_PASSWORD_AUTH=true` (login por senha da API).

Rewrites `/v1/*` e `/health/*` → `BUSINESS_API_INTERNAL_URL`.
